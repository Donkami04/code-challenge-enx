language: python
python:
  - "3.8"
# It will run only in the main branch  
branches:
  only:
    - main
# command to install dependencies
install:
  - pip install -r requirements.txt

# command to run tests
script:
  - pytest 
#  - flake8 update_plan.py 